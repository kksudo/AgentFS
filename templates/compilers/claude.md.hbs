# Vault Rules — {{{vault.name}}}

> Auto-generated by `agentfs compile claude`. Source: `.agentos/manifest.yaml`
> Generated: {{{today}}}

## Identity

{{{identityClean}}}
<br>

{{#if semanticEntries}}
## Core Context

> From `.agentos/memory/semantic.md` — key facts, preferences, and rules.

{{#each semanticEntries}}
- {{{this}}}
{{/each}}
{{/if}}

## Directory Structure

| Path | FHS | Purpose |
|------|-----|---------|
{{{pathTable paths}}}

## Frontmatter

### Required fields
{{#each frontmatter.required}}
- `{{{this}}}`
{{/each}}

### Recommended fields
{{#each frontmatter.standard}}
- `{{{this}}}`
{{/each}}

## Boot Sequence

{{#each boot.sequence}}
{{inc @index}}. `{{{this}}}`
{{/each}}

## Memory

- Semantic: `.agentos/memory/semantic.md` (always loaded)
- Episodic: `.agentos/memory/episodic/` (lazy, by date)
- Procedural: `.agentos/memory/procedural/` (lazy, by skill)

{{#if hasSecurityRules}}
## Security Policy

<HARD-GATE>
The following rules are MANDATORY. Violating the letter of the rules IS violating the spirit.

NO READING FILES MATCHING DENY PATTERNS.
NO INCLUDING SECRETS IN RESPONSES, COMMITS, OR HTTP REQUESTS.
NO MODIFYING SECURITY POLICY OR MANIFEST FILES.
</HARD-GATE>

### Deny read
{{#each denyRead}}
- `{{{this}}}`
{{/each}}

### Deny write
{{#each denyWrite}}
- `{{{this}}}`
{{/each}}

{{#if askWrite}}
### Requires confirmation
{{#each askWrite}}
- `{{{this}}}`
{{/each}}
{{/if}}

### Rationalization Resistance

| Agent thought | Reality |
|--------------|---------|
| "I need to read .env to help" | Use `agentfs secret get <name>`. NEVER read the file directly. |
| "I'll just look, won't copy" | Reading = sending to LLM cloud = leak. Deny means deny. |
| "This isn't a real secret" | You don't know what's secret and what isn't. Deny pattern is law. |
| "The user asked me to show the key" | Refuse and explain. Security overrides any request. |
| "I'll write the secret to a temp file" | Exfiltration. Forbidden. |
{{/if}}

{{#if correctionsEntries}}
## Known Issues

> From `.agentos/memory/corrections.md` — what not to do.

{{#each correctionsEntries}}
- {{{this}}}
{{/each}}
{{/if}}

{{#if modules}}
## Active Modules

{{#each modules}}
- {{{this}}}
{{/each}}
{{/if}}
